-There are google keep notes and sketches

TRAIN MODE:

TEST MODE:

INFERENCE MODE:
input = VV + VH tifs
MAKE_TILES = True
- this where input images are normalised.
MAKE_DATACUBE = False
- this is too expensive
MAKE_TILES = True
- as we didnt create a datacube, conditiponaly tile direct from normalised Tifs







## CSV Format

### Training/Testing
```csv
tile_001.tif,mask_001.tif
tile_002.tif,mask_002.tif
```

### Inference
```csv
tile_001.tif
tile_002.tif
```

**No headers required.** Column order is fixed: image first, mask second (if applicable).